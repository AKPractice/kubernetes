apiVersion: v1
kind: Service
metadata:
  name: lb-service
  labels:
    purpose: lb-demo
spec:
  type: LoadBalancer
  selector:
    purpose: lb-demo
    project: MyProject
    env: dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.service.targetport }}